spring:
  application:
    name: restaurant
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  datasource:
    url: jdbc:mysql://localhost:3306/restaurant?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    web: 
      pageable: 
        one-indexed-parameters: true #page = 1
    redis:
      host: localhost
      port: 6379
  servlet:
    multipart:
      max-request-size: 10MB
      max-file-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: baoluugia567@gmail.com
    password: kummzmnvzgybtsga
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
app:
  mail:
    from: "Nhà hàng ThreeShip <baoluugia567@gmail.com>"
server:
  servlet:
    context-path: /api/v1
fwb: 
  jwt: 
    base64-secret: ${JWT_SECRET}
    accessToken-validity-in-second: 7200 #2 hour
    refreshToken-validity-in-second: 1296000 #15 day
  upload-file:
    base-uri: file:///C:/Users/tavin/Downloads/app_restaurant/upload/
google:
  oauth:
    client-id: ${GOOGLE_CLIENT_ID}

